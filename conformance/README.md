# Conformance

CI harness for federation authoring grammars. **Vectors** live in `guiders-platform` (`Authoring.Conformance`); this folder runs them against LSP/CLI surfaces.

Wave 1: wire `dotnet test` once `AIGuiders.Platform.Authoring.Conformance` ships.
